name: CI-CD

on:
  push:
    branches: ['**']
  pull_request:

jobs:
  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: TypeScript typecheck
        run: npx tsc --noEmit

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build
        env:
          NEXT_PUBLIC_API_BASE_URL: http://127.0.0.1:8000/api/v1
          NEXT_PUBLIC_ENABLE_ANALYTICS: "true"

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      # Runs only fully-mocked specs (no backend required).
      # Auth E2E specs (auth-negative, auth-ui-negative, auth-ui-multi) require
      # a live backend and are validated locally via: make frontend-auth-smoke
      - name: E2E (mocked specs — no backend required)
        run: npm run e2e:ci
        env:
          NEXT_PUBLIC_API_BASE_URL: http://127.0.0.1:8000/api/v1

  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: |
            backend/requirements.txt
            backend/requirements-dev.txt

      - name: Install dependencies
        run: pip install -r requirements-dev.txt

      - name: Run tests
        run: PYTHONPATH=. python -m pytest -q

  deploy:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs: [frontend, backend]
    steps:
      - name: Deploy placeholder
        run: echo "Add deployment command for your cloud target (Vercel/Render/Fly/ECS)."
